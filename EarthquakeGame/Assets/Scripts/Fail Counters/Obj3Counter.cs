using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obj3Counter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LoseScene3.obj3failcounter++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
