using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obj2Counter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LoseScene2.obj2failcounter++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
