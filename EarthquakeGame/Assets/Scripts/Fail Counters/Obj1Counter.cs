using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obj1Counter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LoseScene1.obj1failcounter++;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
